__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Real config with bridge credentials - never commit this
config.yaml

# Runtime artifacts
*.fifo
huesync.db
cava_0.10.7+dfsg-1_amd64.deb

# Test audio files — binary, too large for the repo
tests/*.flac
tests/*.wav
tests/*.mp3
